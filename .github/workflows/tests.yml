name: Tests

on:
  push:
    branches:
      - main
      - master
      - dev/*
  pull_request:
    branches:
      - main
      - master
      - dev/*

jobs:
  call-tests:
    name: Extension tests
    uses: phpbb-extensions/test-framework/.github/workflows/tests.yml@3.3.x
    with:
      EXTNAME: vinny/wysiwyg
      RUN_COMPOSER_INSTALL: '1'
      EPV: '0'
      RUN_FUNCTIONAL_TESTS: '0'
